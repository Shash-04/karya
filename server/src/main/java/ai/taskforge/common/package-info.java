/**
 * Shared building blocks used across features: the centralized
 * {@code ApiResponse<T>} envelope, domain exceptions, the global
 * {@code @RestControllerAdvice} exception handler, and base entity types.
 */
package ai.taskforge.common;
